---
import { Image } from 'astro:assets';
import img1 from '../assets/images/new-roof-slate-tiles-british-house.webp';
import img2 from '../assets/images/roof-repair-replacing-broken-tiles.webp';
import img3 from '../assets/images/flat-roof-epdm-rubber-membrane.webp';
import img4 from '../assets/images/guttering-fascias-upvc-british-house.webp';
import img5 from '../assets/images/chimney-repair-repointing-leadwork.webp';
import img6 from '../assets/images/emergency-roof-repair-storm-damage.webp';

interface Props {
  phone?: string;
}

const { phone = '01225 985713' } = Astro.props;

const gallery = [
  { src: img1, alt: 'New slate roof fitted on a British house' },
  { src: img2, alt: 'Roof repair replacing broken tiles' },
  { src: img3, alt: 'Flat roof EPDM rubber membrane installation' },
  { src: img4, alt: 'New UPVC guttering and fascia boards' },
  { src: img5, alt: 'Chimney repair and repointing' },
  { src: img6, alt: 'Emergency roof repair after storm damage' },
];
---

<section class="section section--off-white">
  <div class="container">
    <div class="about-split">
      <div class="about-split__image about-split__slider">
        <div class="about-slider__track">
          {gallery.map((img) => (
            <div class="about-slider__slide">
              <Image
                src={img.src}
                alt={img.alt}
                widths={[400, 600]}
                loading="lazy"
                class="about-slider__img"
              />
            </div>
          ))}
        </div>
        <button class="about-slider__arrow about-slider__arrow--prev" aria-label="Previous slide">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>
        </button>
        <button class="about-slider__arrow about-slider__arrow--next" aria-label="Next slide">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
        </button>
        <div class="about-slider__dots"></div>
      </div>
      <div class="about-split__content">
        <h2>Welcome to Majestic Services LTD</h2>
        <p>Majestic Services are roofing experts based in Bath, we're a fully certified, family-run business with over 20 years of experience delivering quality workmanship and a personal service.</p>
        <p>We don't believe in hidden charges or call-out fees. You'll get a detailed written quote before any work starts, and every job is backed by our 15-year warranty.</p>
        <ul class="about-split__checks">
          <li>Fully insured and qualified team</li>
          <li>Free pre-work survey and inspection</li>
          <li>15-year workmanship warranty</li>
          <li>No call-out fees, no hidden costs</li>
          <li>24/7 emergency availability</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<script>
  const slider = document.querySelector('.about-split__slider');
  if (slider) {
    const slides = slider.querySelectorAll('.about-slider__slide');
    const dotsContainer = slider.querySelector('.about-slider__dots');
    const prevBtn = slider.querySelector('.about-slider__arrow--prev');
    const nextBtn = slider.querySelector('.about-slider__arrow--next');
    const total = slides.length;
    let current = 0;
    let interval: ReturnType<typeof setInterval>;

    slides[0]?.classList.add('active');

    slides.forEach((_, i) => {
      const dot = document.createElement('button');
      dot.className = `about-slider__dot${i === 0 ? ' active' : ''}`;
      dot.setAttribute('aria-label', `Show image ${i + 1}`);
      dot.addEventListener('click', () => { stopAuto(); goTo(i); startAuto(); });
      dotsContainer?.appendChild(dot);
    });

    const dots = dotsContainer?.querySelectorAll('.about-slider__dot');

    function goTo(index: number) {
      current = ((index % total) + total) % total;
      slides.forEach((s, i) => s.classList.toggle('active', i === current));
      dots?.forEach((d, i) => d.classList.toggle('active', i === current));
    }

    function startAuto() {
      interval = setInterval(() => goTo(current + 1), 4000);
    }

    function stopAuto() {
      clearInterval(interval);
    }

    prevBtn?.addEventListener('click', () => { stopAuto(); goTo(current - 1); startAuto(); });
    nextBtn?.addEventListener('click', () => { stopAuto(); goTo(current + 1); startAuto(); });

    startAuto();
  }
</script>
